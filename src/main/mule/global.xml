<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
	xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:http="http://www.mulesoft.org/schema/mule/http"
	xmlns:apikit="http://www.mulesoft.org/schema/mule/mule-apikit"
	xmlns:os="http://www.mulesoft.org/schema/mule/os"
	xmlns:secure-properties="http://www.mulesoft.org/schema/mule/secure-properties"
	xmlns:api-gateway="http://www.mulesoft.org/schema/mule/api-gateway"
	xsi:schemaLocation="
		http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
		http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
		http://www.mulesoft.org/schema/mule/mule-apikit http://www.mulesoft.org/schema/mule/mule-apikit/current/mule-apikit.xsd
		http://www.mulesoft.org/schema/mule/os http://www.mulesoft.org/schema/mule/os/current/mule-os.xsd
		http://www.mulesoft.org/schema/mule/secure-properties http://www.mulesoft.org/schema/mule/secure-properties/current/mule-secure-properties.xsd
		http://www.mulesoft.org/schema/mule/api-gateway http://www.mulesoft.org/schema/mule/api-gateway/current/mule-api-gateway.xsd">

	<!-- Global Property for Environment -->
	<global-property name="mule.env" value="dev" doc:name="Environment Property"/>
	<global-property name="secure.key" value="${secure.key}" doc:name="Secure Key Property"/>

	<!-- Secure Properties Configuration -->
	<secure-properties:config name="Secure_Properties_Config" doc:name="Secure Properties Config" file="config-secure.yaml" key="${secure.key}">
		<secure-properties:encrypt algorithm="AES" mode="CBC"/>
	</secure-properties:config>

	<!-- Configuration Properties -->
	<configuration-properties doc:name="Configuration Properties" file="config.yaml"/>

	<!-- HTTP Listener Configuration -->
	<http:listener-config name="HTTP_Listener_Config" doc:name="HTTP Listener Config">
		<http:listener-connection host="${http.listener.host}" port="${http.listener.port}">
			<reconnection>
				<reconnect frequency="3000" count="3"/>
			</reconnection>
		</http:listener-connection>
	</http:listener-config>

	<!-- HTTP Request Configuration for ERP System -->
	<http:request-config name="HTTP_Request_Config_ERP" doc:name="HTTP Request Config ERP" responseTimeout="${erp.responseTimeout}">
		<http:request-connection host="${erp.host}" port="${erp.port}" protocol="HTTPS">
			<reconnection>
				<reconnect frequency="5000" count="3"/>
			</reconnection>
		</http:request-connection>
	</http:request-config>

	<!-- HTTP Request Configuration for WMS System -->
	<http:request-config name="HTTP_Request_Config_WMS" doc:name="HTTP Request Config WMS" responseTimeout="${wms.responseTimeout}">
		<http:request-connection host="${wms.host}" port="${wms.port}" protocol="HTTPS">
			<reconnection>
				<reconnect frequency="5000" count="3"/>
			</reconnection>
		</http:request-connection>
	</http:request-config>

	<!-- APIKit Router Configuration -->
	<apikit:config name="wms-erp-api-config" doc:name="APIKit Config" api="api/wms-erp-api.raml" outboundHeadersMapName="outboundHeaders" httpStatusVarName="httpStatus"/>

	<!-- Object Store for Idempotency -->
	<os:object-store name="Idempotency_Object_Store" doc:name="Idempotency Object Store" persistent="true" entryTtl="${objectstore.idempotency.entryTtl}" entryTtlUnit="${objectstore.idempotency.entryTtlUnit}" maxEntries="${objectstore.idempotency.maxEntries}"/>

	<!-- Object Store for Batch Processing -->
	<os:object-store name="Batch_Object_Store" doc:name="Batch Object Store" persistent="true" entryTtl="${objectstore.batch.entryTtl}" entryTtlUnit="${objectstore.batch.entryTtlUnit}" maxEntries="${objectstore.batch.maxEntries}"/>

	<!-- Object Store for Failed Records -->
	<os:object-store name="Failed_Records_Object_Store" doc:name="Failed Records Object Store" persistent="true" entryTtl="168" entryTtlUnit="HOURS" maxEntries="100000"/>

	<!-- API Autodiscovery Configuration -->
	<api-gateway:autodiscovery apiId="${api.id}" flowRef="wms-erp-api-main" doc:name="API Autodiscovery"/>

	<!-- Global Configuration with Default Error Handler Reference -->
	<configuration defaultErrorHandler-ref="Global_Error_Handler" doc:name="Configuration"/>

</mule>
